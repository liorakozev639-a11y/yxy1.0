// Copy this file to frontend/config.js before publishing static frontend files.
// Use HTTPS when the backend is publicly available.
(function (root) {
  if (!root) return;
  root.FREE_TIME_API_BASE_URL = 'https://<your-domain>';
}(typeof window !== 'undefined' ? window : null));

